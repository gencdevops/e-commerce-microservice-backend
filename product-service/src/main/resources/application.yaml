spring:
  cache:
    type: redis

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    prometheus:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true
  #  prometheus:
  #    metrics:
  #      export:
  #        enabled: true
aws:
  bucket-name: 'advivemy-images'
  access-key: 'AKIAYHLZLDRDSWZZIDPD'
  secret-key: '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'